server {
    # Cloud Run의 기본 포트인 8080으로 수신
    listen 8080;
    server_name _;

    # 정적 파일이 위치한 경로
    root /usr/share/nginx/html;
    index index.html;

    # React Router 등을 사용할 때 새로고침 시 404 에러 방지
    location / {
        try_files $uri $uri/ /index.html;
    }
}
